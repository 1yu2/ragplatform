# Frontend (React + Vite)

## Run

```bash
cd frontend
npm install
npm run dev
```

## API Base URL

Use env var:

```bash
VITE_API_BASE_URL=http://127.0.0.1:8000
```

Default fallback is `http://127.0.0.1:8000`.

## Pages

- `/studio`
- `/gallery`
- `/metrics`
